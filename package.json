{
  "name": "autobahn",
  "version": "1.0.0",
  "description": "Autobahn - Outrun-style mobile highway racing game with gyroscope controls",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "postinstall": "node --input-type=module -e \"if (process.env.WORKERS_CI === '1') { const { execSync } = await import('node:child_process'); execSync('npx vite build', { stdio: 'inherit' }); }\""
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
